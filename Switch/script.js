/**
1. To avoid to many nested if else 
2. Each case it checked stictly.

 */
const data=prompt("Enter Your Age");

switch (data) {
    case "18":
        
        break;

    default:
        break;
}